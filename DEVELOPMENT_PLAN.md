# แผนพัฒนาม็อดภาษาไทย Security 51

## Goal

พัฒนาม็อดภาษาไทยสำหรับเกม **Security 51** ให้ข้อความที่ผู้เล่นเห็นแสดงผลเป็นภาษาไทยอย่างถูกต้อง อ่านง่าย และคงความหมายกับบรรยากาศของเกม โดยต้องรักษา key, placeholder, rich-text tag และเงื่อนไขของข้อความเดิม รองรับฟอนต์ไทยและการวางสระ/วรรณยุกต์ ติดตั้งและถอนการติดตั้งได้อย่างปลอดภัย และผ่านการทดสอบกับเกมเวอร์ชันปัจจุบัน

### เกณฑ์ที่ถือว่า Goal สำเร็จ

- เมนู หน้าตั้งค่า บทสนทนา เอกสาร คำเตือน และข้อความสำคัญระหว่างเล่นได้รับการแปลครบตามขอบเขตที่ตรวจพบ
- ไม่มี key สูญหาย ข้อความสลับตำแหน่ง หรือ placeholder/rich-text tag เสียหาย
- ภาษาไทยไม่เป็นสี่เหลี่ยม ไม่เป็นข้อความ encoding เสีย และสระ/วรรณยุกต์ไม่หลุดจนอ่านไม่ได้
- ข้อความไม่ล้นหรือถูกตัดในหน้าจอสำคัญ หรือมีการปรับคำ/ขนาดตัวอักษรเพื่อแก้ไขแล้ว
- เกมเปิด เล่น บันทึก และโหลดเกมได้โดยไม่เกิดข้อผิดพลาดจากม็อด
- มีตัวติดตั้งหรือขั้นตอนติดตั้งที่ทำซ้ำได้ พร้อมวิธีถอนและคืนไฟล์เดิม
- มีรายงานผลทดสอบและระบุเวอร์ชันเกมที่รองรับ

## ข้อมูลทางเทคนิคที่ยืนยันแล้ว

- เกมสร้างด้วย Unity 6 รุ่น `6000.3.8f1`
- เกมเป็น IL2CPP และมี `GameAssembly.dll` กับ `global-metadata.dat`
- เกมมี I2 Localization (`I2Loc.dll`) และฐานภาษาอยู่ใน Unity assets
- ส่วนแสดงข้อความใช้ TextMeshPro จึงต้องจัดการ font asset และ fallback สำหรับภาษาไทย
- วิธีเดิมที่ใช้ BepInEx ล้มเหลวเพราะปน `Unhollower` รุ่นเก่ากับ `Il2CppInterop` รุ่นใหม่
- Steam Verify คืนไฟล์เกมทางการ แต่ไม่ลบไฟล์ส่วนเกินจากม็อดเดิม

## หลักการพัฒนา

1. ไม่แก้ไฟล์เกมโดยไม่มีสำเนาต้นฉบับและ checksum
2. พิสูจน์การแสดงข้อความและฟอนต์ไทยด้วยต้นแบบเล็กก่อนแปลทั้งเกม
3. เก็บข้อความต้นฉบับ คำแปล และไฟล์ที่ build แล้วแยกจากกัน
4. ทุกการแก้คำแปลต้องตรวจ placeholder, tag และบรรทัดอัตโนมัติ
5. ตัวติดตั้งต้องตรวจเวอร์ชันและสำรองไฟล์ก่อนเขียนทับ
6. หลีกเลี่ยงการพึ่งพาการแปลออนไลน์ระหว่างเล่นเกม

## แผนการพัฒนา

### Phase 0 — เตรียมสภาพแวดล้อมสะอาด

- บันทึก build ID และ checksum ของไฟล์เกมหลัก
- แยกไฟล์ม็อดเดิมที่ Steam ไม่ลบออกจากโฟลเดอร์เกม โดยเก็บไว้เป็นหลักฐานนอกโฟลเดอร์เกม
- เปิดเกมแบบไม่มีม็อดเพื่อยืนยันว่าเกมทำงานตามปกติ
- สร้างโครงสร้างโฟลเดอร์สำหรับ source, tools, extracted data, translation, build และ test report

**เกณฑ์ผ่าน:** เกมเปิดได้ตามปกติ และมี baseline/checksum ที่สามารถใช้ตรวจสอบการเปลี่ยนแปลง

### Phase 1 — สำรวจและสกัดฐานข้อความ

- ตรวจ `resources.assets`, `globalgamemanagers.assets` และ shared assets ที่เกี่ยวข้อง
- ระบุ I2 LanguageSource, language list, terms และตำแหน่งข้อความทั้งหมด
- ส่งออกข้อมูลต้นฉบับเป็นรูปแบบกลาง เช่น CSV หรือ JSON แบบ UTF-8
- จำแนกข้อความเป็นเมนู บทสนทนา เอกสาร tutorial ระบบ และข้อความที่ไม่ควรแปล
- ตรวจข้อความที่ประกอบระหว่าง runtime หรือฝังอยู่ในโค้ด IL2CPP

**เกณฑ์ผ่าน:** สกัด key และข้อความภาษาอังกฤษได้อย่างทำซ้ำได้ พร้อมจำนวนรายการและแหล่งที่มาชัดเจน

### Phase 2 — Proof of Concept ภาษาไทย

- เลือกข้อความทดสอบ 10–20 รายการจากหลายหน้าจอ
- เพิ่มหรือแทน language slot สำหรับภาษาไทยโดยไม่ทำลายภาษาอื่น
- สร้าง TextMeshPro font asset ภาษาไทยจากฟอนต์ที่อนุญาตให้นำไปแจกจ่าย
- ตั้ง fallback font และทดสอบพยัญชนะ สระบน/ล่าง วรรณยุกต์ ตัวเลข และเครื่องหมาย
- build ม็อดต้นแบบและทดสอบในเมนูและระหว่างเล่นจริง

**เกณฑ์ผ่าน:** เกมแสดงข้อความไทยจริงอย่างน้อยสองบริบท ไม่มีตัวสี่เหลี่ยม เกมไม่ crash และภาษาเดิมยังใช้งานได้

**จุดตัดสินใจ:** หาก asset patch ไม่เสถียร ให้ทดลองปลั๊กอิน IL2CPP ที่รองรับ Unity 6 โดยใช้ชุด loader รุ่นเดียวกันทั้งหมด

### Phase 3 — สร้างระบบงานแปล

- กำหนด style guide: สรรพนาม ระดับภาษา ชื่อหน่วยงาน ศัพท์วิทยาศาสตร์ และศัพท์ Area 51/SCP
- สร้าง glossary และ translation memory
- เพิ่ม validator สำหรับ key ซ้ำ ช่องว่าง placeholder tag ขึ้นบรรทัด และ encoding
- กำหนดสถานะข้อความ เช่น `untranslated`, `draft`, `reviewed`, `in-game verified`
- ป้องกันการแก้ไข key และข้อความควบคุมโดยไม่ตั้งใจ

**เกณฑ์ผ่าน:** สามารถแก้คำแปล ตรวจความถูกต้อง และ build ใหม่ได้โดยไม่แก้ assets ด้วยมือทุกครั้ง

### Phase 4 — แปลเนื้อหาทั้งเกม

- แปล UI และข้อความระบบก่อนเพื่อใช้ทดสอบได้สะดวก
- แปล tutorial กฎการตรวจ เอกสาร และข้อความที่มีผลต่อ gameplay
- แปลบทสนทนาและเนื้อเรื่องตามลำดับการเล่น
- ตรวจความสอดคล้องของชื่อบุคคล ตำแหน่ง หน่วยงาน และคำใบ้
- ทบทวนภาษาไทยรอบแรกและรอบบรรณาธิการ

**เกณฑ์ผ่าน:** ไม่มีข้อความที่ผู้เล่นเห็นค้างเป็นอังกฤษโดยไม่อยู่ในรายการยกเว้น และรายการทั้งหมดผ่าน validator

### Phase 5 — QA ในเกม

- ทดสอบความละเอียดและอัตราส่วนหน้าจอที่ใช้งานบ่อย
- ตรวจเมนู HUD เอกสาร บัตร บทสนทนา tutorial subtitle และหน้าจอจบเกม
- ตรวจข้อความยาว การตัดบรรทัด การล้นกรอบ และการซ้อนของสระ/วรรณยุกต์
- ทดสอบเส้นทางการเล่น ตัวเลือก และฉากจบหลายแบบเท่าที่ทำได้
- ทดสอบ save/load, achievement และ Steam Overlay
- บันทึก screenshot พร้อม issue ID สำหรับจุดที่ต้องแก้

**เกณฑ์ผ่าน:** ไม่มีปัญหาระดับ blocker/critical และปัญหา UI สำคัญได้รับการแก้หรือมีข้อจำกัดระบุไว้

### Phase 6 — Packaging และเผยแพร่

- สร้างแพ็กเกจที่มีเฉพาะไฟล์จำเป็น ห้ามรวมไฟล์เกมต้นฉบับที่มีลิขสิทธิ์เกินความจำเป็น
- ทำ installer ที่ตรวจ path, build ID และ checksum ก่อนติดตั้ง
- สำรองไฟล์ที่จะถูกเปลี่ยน และทำ uninstaller/restore ที่ทดสอบแล้ว
- เขียน README วิธีติดตั้ง ถอนการติดตั้ง อัปเดต และแก้ปัญหา
- ใส่ license/เครดิตของฟอนต์ เครื่องมือ และผู้แปล
- สร้าง release archive พร้อม checksum

**เกณฑ์ผ่าน:** ติดตั้งและถอนได้บนเกมสะอาด ผ่านการทดสอบซ้ำ และไม่ทิ้งเกมไว้ในสภาพเปิดไม่ได้

### Phase 7 — รองรับการอัปเดตเกม

- เปรียบเทียบ assets และฐานข้อความเมื่อ build ID เปลี่ยน
- ตรวจ key เพิ่ม ลบ หรือแก้ไข และนำคำแปลเดิมกลับมาใช้โดยไม่ทับข้อมูลใหม่
- rebuild ฟอนต์หรือ assets เมื่อ Unity serialization เปลี่ยน
- ระบุ compatibility ของม็อดทุก release

## โครงสร้างโปรเจกต์ที่เสนอ

```text
Security51Modthai/
├─ docs/                 เอกสาร การสำรวจ และ style guide
├─ tools/                สคริปต์ extract, validate, build และ package
├─ source/               manifest และข้อมูลที่จำเป็นต่อการ build
├─ translations/
│  ├─ source_en/         ข้อความต้นฉบับที่สกัดแล้ว
│  ├─ th/                คำแปลภาษาไทย
│  └─ glossary/          ศัพท์และแนวทางการใช้ภาษา
├─ fonts/                source/license/config ของฟอนต์ที่แจกจ่ายได้
├─ build/                ผลลัพธ์ที่สร้างขึ้นและไม่ควรแก้ด้วยมือ
├─ tests/                validator และข้อมูลทดสอบ
└─ reports/              รายงาน extraction, coverage และ in-game QA
```

## ความเสี่ยงและวิธีรับมือ

| ความเสี่ยง | ผลกระทบ | วิธีรับมือ |
|---|---|---|
| Unity 6 เปลี่ยน serialization | เครื่องมือ assets รุ่นเก่าอ่าน/เขียนไม่ได้ | ทดสอบกับสำเนา ใช้เครื่องมือที่รองรับ Unity 6 และตรวจ checksum/build ทุกครั้ง |
| IL2CPP loader ไม่รองรับ | เกมเปิดไม่ได้หรือ plugin ไม่ทำงาน | ใช้ asset patch เป็นทางหลัก และทดสอบ loader รุ่นใหม่แบบแยกสภาพแวดล้อมเมื่อจำเป็น |
| ฟอนต์ไม่มี glyph ไทย | ตัวสี่เหลี่ยมหรืออักขระหาย | ใช้ฟอนต์ไทยที่ครบ สร้าง TMP atlas/fallback และมีชุดข้อความทดสอบ glyph |
| การจัดวางภาษาไทยใน TMP | สระ/วรรณยุกต์ซ้อนหรือเยื้อง | เลือกฟอนต์เหมาะสม ทดสอบหลายขนาด และปรับ metrics/fallback |
| ข้อความไทยยาวกว่าอังกฤษ | ล้นกรอบหรือบัง UI | ใช้คำกระชับ ปรับ line break/auto-size เฉพาะจุด และตรวจด้วย screenshot |
| placeholder หรือ tag เสีย | เกมแสดงค่าผิดหรือ error | ใช้ validator เปรียบเทียบ token ระหว่างต้นฉบับกับคำแปล |
| เกมอัปเดต | ม็อดใช้ไม่ได้หรือเขียนทับไฟล์ผิดรุ่น | ตรวจ build ID/checksum และหยุด installer เมื่อเวอร์ชันไม่ตรง |
| ลิขสิทธิ์ไฟล์เกม/ฟอนต์ | แจกจ่ายแพ็กเกจไม่ได้ | แจกเฉพาะ patch/delta และใช้ฟอนต์ที่มี license อนุญาต |

## Milestones

- **M1 — Clean baseline (ผ่าน):** เกมสะอาดและมี checksum
- **M2 — Extracted corpus (ผ่าน):** ดึงฐานข้อความต้นฉบับสำเร็จ
- **M3 — Thai PoC (รอตรวจภาพ):** runtime ฉีดคำแปลครบ `3,457/3,457` รายการ และสร้าง fallback จาก Noto Sans Thai แบบพกพาสำเร็จโดยไม่มี plugin error เหลือตรวจหน้าจอจริงอย่างน้อยสองบริบท
- **M4 — Translation pipeline (ผ่าน):** extract/validate/build ทำซ้ำได้ และรอบล่าสุดผ่าน validator โดยไม่มี warning/error
- **M5 — Content complete (ผ่าน):** มีค่า Thai แล้ว `3,481/3,481` รายการที่ต้องแสดงใน build `25104142` (`100%`); เหลือยกเว้นเพียง URL และค่า `#N/A`/fragment ที่ไม่ใช่ข้อความใช้งาน
- **M6 — QA complete:** ผ่านการทดสอบในเกม
- **M7 — Release candidate:** ติดตั้ง/ถอน/อัปเดตได้อย่างปลอดภัย

### ระบบเวอร์ชันที่ใช้งานแล้ว

- รุ่นม็อดปัจจุบัน: `0.1.0-beta`
- Steam build ที่รองรับ: `25104142`
- แหล่งข้อมูลกลาง: `version.json`
- ชื่อ release: `Security51ThaiMod-v{modVersion}-game{buildId}`
- ประวัติรุ่น: `CHANGELOG.md`
- ขั้นตอนรองรับเกมอัปเดต: `docs/GAME_UPDATE_WORKFLOW.md`
- ตัวตรวจ build/hash: `tools/Check-Game-Version.ps1`
- versioned install/uninstall cycle: ผ่าน (`VERSIONED_INSTALL_UNINSTALL_OK`)

## งานถัดไป

1. เปิดเกมแบบมองเห็นและบันทึกภาพข้อความไทยจากอย่างน้อยสองบริบท
2. ตรวจ glyph การวางสระ/วรรณยุกต์ การตัดบรรทัด และข้อความล้นกรอบ
3. ตรวจภาพจริงของ `Noto Sans Thai` แบบพกพาที่รวมในแพ็กเกจ (runtime โหลดจากไฟล์โดยตรง และยังมีฟอนต์ Windows เป็น fallback สำรอง)
4. ทบทวนคำแปล UI 204 รายการในเกม แล้วเปลี่ยนสถานะจาก `draft` ตามหลักฐาน
5. ทดสอบ installer/uninstaller แบบ mod-only บนโครงสร้างจำลองแล้ว ขั้นถัดไปคือตรวจซ้ำบนสำเนาเกมสะอาดทั้งชุดและ path รูปแบบอื่น
6. ทบทวน `Story_Dial` ที่แปลฉบับร่างครบ 386/386 ในเกม โดยตรวจชื่อผู้พูด น้ำเสียง บริบท และข้อความล้นกรอบ
7. `Real_Dev` แปลฉบับร่างครบ `455/455` แล้ว ให้ตรวจอาการ ชื่อสิ่งมีชีวิต ไพ่ และศัพท์แต่งกับภาพ/บริบทจริง
8. `DM_Ends` แปลฉบับร่างครบ `515/515` แล้ว ให้ตรวจน้ำเสียงหลายบุคลิก จังหวะบทสนทนา และความยาวกล่องข้อความในเกมจริง
9. `NewsAndReports` แปลฉบับร่างครบ 133/133 แล้ว ให้ตรวจ `[redacted]` น้ำเสียงรายงาน และข้อความยาวบนหน้าจอจริง
10. `Rorschach` แปลฉบับร่างครบ 133/133 แล้ว ให้ตรวจความยาวตัวเลือกและคำกำกวมกับภาพจริง
11. `Doc_Loc` แปลฉบับร่างครบ 342/342 แล้ว ให้ตรวจชื่อยาว การตัดบรรทัด และศัพท์จุลชีพในหน้าจอจริง
12. `CityOperations` แปลฉบับร่างครบ 287/287 แล้ว ให้ตรวจคำอธิบายยาว รหัสโหนด และ placeholder ในรายงานปฏิบัติการจริง

## สถานะยืนยันล่าสุด

- Steam build: `25104142`
- ค่า localization รุ่น `0.1.1`: `3,481/3,481` รายการที่ต้องแสดง (`100%`); เพิ่มชื่อภาษาเจ้าของภาษาและ `Steam Deck` เพื่อป้องกัน raw key หลุดบน UI
- Runtime ล่าสุด: marker `4/4`, โหลดและฉีด `3,457/3,457`, plugin error `0`
- Release draft และ installer/uninstaller สร้างได้และผ่านการทดสอบในโครงสร้าง Steam จำลอง
- Full-copy install/runtime/uninstall cycle ผ่านบน path ภาษาไทยสำหรับ build `25104142`; payload ถูกลบครบและไฟล์เกมทางการหลัก 13 ไฟล์มี hash ตรง baseline หลังถอน
- สกัดฐานข้อความ build `25104142` ใหม่แล้ว พบข้อความใหม่/เปลี่ยน 69 รายการและแปลครบทั้งหมด โดยรักษาคำแปล fallback เดิม 287 รายการ
- Goal ยังไม่สำเร็จจนกว่าจะตรวจภาพ/บริบทจริง โดยเฉพาะ glyph การซ้อนสระ การตัดบรรทัด และข้อความล้นกรอบ
- สร้าง versioned release `Security51ThaiMod-v0.1.0-game25104142` แล้ว โดย build script อ่าน compatibility จาก `version.json` และปฏิเสธ build ที่ไม่ได้ทำเครื่องหมายรองรับ
