# รายงานย้ายฐานข้อความไป Steam build 25104142

วันที่ตรวจ: 3 กันยายน 2026

## ผลการเปรียบเทียบ

- AssetRipper 2.0.0 สกัด `I2Languages` จาก `resources.assets` ของ build `25104142`
- ฐานใหม่มี 5,476 แถว, 4,954 unique keys และ 3,484 keys ที่มีข้อความอังกฤษ
- เพิ่ม 67 keys โดยไม่มี key เดิมถูกลบ
- 287 keys ของตัวละคร Six ที่เคยอาศัย Russian fallback ได้รับข้อความอังกฤษอย่างเป็นทางการใน build ใหม่
- Rorschach เปลี่ยนความหมาย 2 keys: `Polar Bear` → `Big Bear` และ `Kombucha` → `Creepy Mushroom`
- สคริปต์ migration รักษาคำแปลเดิมตาม key และรีเซ็ตเฉพาะข้อความที่เปลี่ยนความหมาย

## ผลหลังแปล

- แปลแล้ว `3,457/3,457` keys ที่มีสิทธิ์แปล (`100%`)
- อีก 27 keys ที่มีข้อความเป็นรายการยกเว้น เช่น ชื่อภาษา URL และชื่อผลิตภัณฑ์
- validator ทั้งฐาน: warning 0, error 0
- runtime ของฐานใหม่: marker `4/4`, โหลดและฉีด `3,457/3,457`, plugin error 0

ไฟล์ต้นฉบับและตารางคำแปลของ build ก่อนหน้าถูกเก็บด้วย suffix `.24972648` เพื่อใช้ตรวจย้อนหลัง
